{
  "name": "tiny-project",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "vite --jsx-factory=h",
    "build": "parcel build *.html",
    "cypress": "cypress open",
    "test": "yarn start-test 'yarn start' 3000 'cypress run'"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged",
      "pre-push": "yarn test"
    }
  },
  "dependencies": {
    "preact": "^10.4.1",
    "start-server-and-test": "^1.11.0"
  },
  "devDependencies": {
    "cypress": "^4.5.0",
    "husky": "^4.2.5",
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.0.5",
    "pretty-quick": "^2.0.1",
    "typescript": "^3.8.3",
    "vite": "^0.11.0"
  }
}
